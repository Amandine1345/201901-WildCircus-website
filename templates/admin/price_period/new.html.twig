{% extends 'base_admin.html.twig' %}

{% block title %}New Period{% endblock %}

{% block body %}
    <h1>Create new Period</h1>
    <p>
        <a href="{{ path('price_index') }}" role="button" class="btn btn-secondary">
            <i class="fas fa-chevron-left mr-2"></i> Back to list
        </a>
    </p>

    {{ include('/admin/price_period/_form.html.twig') }}
{% endblock %}
