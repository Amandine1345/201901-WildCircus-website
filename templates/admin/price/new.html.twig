{% extends 'base_admin.html.twig' %}

{% block title %}New Price - {{ parent() }}{% endblock %}

{% block body %}
    <h1>Create new Price</h1>
    <p>
        <a href="{{ path('price_index') }}" role="button" class="btn btn-secondary">
            <i class="fas fa-chevron-left mr-2"></i> Back to list
        </a>
    </p>

    {{ include('admin/price/_form.html.twig') }}
{% endblock %}
