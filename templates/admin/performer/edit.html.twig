{% extends 'base_admin.html.twig' %}

{% block title %}Edit Performer - {{ parent() }}{% endblock %}

{% block body %}
    <h1>Edit Performer</h1>
    <div class="row">
        <div class="col-6">
            <a href="{{ path('performer_index') }}" role="button" class="btn btn-secondary">
                <i class="fas fa-chevron-left mr-2"></i> Back to list
            </a>
        </div>
        <div class="col-6 text-right">
            {{ include('admin/performer/_delete_form.html.twig') }}
        </div>
    </div>

    {{ include('admin/performer/_form.html.twig', {'button_label': 'Update'}) }}

{% endblock %}
