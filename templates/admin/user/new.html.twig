{% extends 'base_admin.html.twig' %}

{% block title %}New User - {{ parent() }}{% endblock %}

{% block body %}
    <h1>Create new User</h1>
    <p>
        <a href="{{ path('user_index') }}" role="button" class="btn btn-secondary">
            <i class="fas fa-chevron-left mr-2"></i> Back to list
        </a>
    </p>

    {{ include('admin/user/_form.html.twig') }}
{% endblock %}
