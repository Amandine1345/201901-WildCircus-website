{% extends 'base_admin.html.twig' %}

{% block title %}Performance index - {{ parent() }}{% endblock %}

{% block body %}
    <h1>Performance index</h1>
    <p class="text-right">
        <a href="{{ path('performance_new') }}" role="button" class="btn btn-danger">
            <i class="fas fa-dumbbell mr-2"></i>Create new
        </a>
    </p>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>Picture</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for performance in performances %}
            <tr>
                <td>{{ performance.id }}</td>
                <td>{{ performance.name }}</td>
                <td>{{ performance.description }}</td>
                <td>{{ performance.picture }}</td>
                <td>
                    <a href="{{ path('performance_edit', {'id': performance.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
